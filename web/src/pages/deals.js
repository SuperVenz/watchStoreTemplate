import React from "react";
import styled from "styled-components";
import Layout from "../components/Layout";
import DealCard from "../components/mutations/DealCard";
function deals({ data }) {
  return (
    <Layout>
      <h1>Fresh Deals</h1>
    </Layout>
  );
}

export default deals;
